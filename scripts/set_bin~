#! /bin/bash

if [ ! -z "$1"];
then
    echo "No source"
fi

if [ ! -z "$2"];
then
    echo "No dest"
fi

echo $src_path=$(which $1)

IFS='/' read -ra ADDR <<< "$2"
for i in "${ADDR[@]}"; do
    echo $i
done
